<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Order Confirmation - Ktebi</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .order-details {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        .table th, .table td {
            padding: 10px;
            border-bottom: 1px solid #dee2e6;
            text-align: left;
        }
        .total {
            font-weight: bold;
            text-align: right;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Thank You for Your Order!</h1>
        <p>Your order has been confirmed and is being processed.</p>
    </div>

    <div class="order-details">
        <h2>Order Details</h2>
        <p><strong>Order ID:</strong> #{{ order.id }}</p>
        <p><strong>Order Date:</strong> {{ order.createdAt|date('F j, Y') }}</p>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Book</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for item in order.orderItems %}
                <tr>
                    <td>{{ item.book.title }}</td>
                    <td>{{ item.quantity }}</td>
                    <td>${{ item.price|number_format(2) }}</td>
                    <td>${{ (item.price * item.quantity)|number_format(2) }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <p class="total">Total Amount: ${{ order.totalPrice|number_format(2) }}</p>

    <div class="footer">
        <p>Thank you for shopping with Ktebi!</p>
        <p>If you have any questions, please contact us at contact@ktebi.com</p>
    </div>
</body>
</html> 